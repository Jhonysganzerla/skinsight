/**
 * CSFloat content script. v0.1: presence stub.
 * v0.2 wires Arbitrage analyzer here (src/modules/arbitrage/analyzer.ts)
 *   + pending-payload banner reading chrome.storage.local.
 */
console.debug('[Skinsight] loaded on csfloat');
